# Codex 자동 비평 요청 — ADMIN_UI_SKELETON_PLAN cycle$N

## 당신의 역할

당신은 시니어 소프트웨어 아키텍트 검수자다. 본 monorepo에서 진행 중인 Phase 0 Week 4 작업 산출물 `docs/decisions/ADMIN_UI_SKELETON_PLAN.md` 를 **냉정하게 비평**한다.

## 비평 운영 원칙

- 5사이클 고정이 아닌 **closeableAfterPatch 신호 누적 → 수렴** 기준.
- 누계 지적 수가 줄어들고 closeableAfterPatch=true 비율이 높아지면 `ready_for_acceptance=true` 신호로 종결한다.
- 본 monorepo의 다른 milestone 누적 패턴: cycle별 10~50 지적 (8 Feature spec 평균 ~71 지적 · 인프라 v1.0 36 지적 · Spike 평균 14~33 지적 · M0 schema v1.0 23 지적).
- **얕은 trivia (typo · 표 정렬 등) 보다 실질 결함을 우선 잡는다** — package 실 코드와 plan 명세 간 불일치 · SoT cascade 누락 · skeleton 범위 vs deferred 분류 합리성 · 인증·RLS 통합 명세의 hole · 운영 시나리오 미커버.

## SoT (반드시 직접 읽고 검증)

본 monorepo의 working root에서 직접 파일을 읽어 plan과 대조한다. 절대 plan 본문만 보고 비평하지 말 것.

- `docs/decisions/ADMIN_UI_SKELETON_PLAN.md` v0.1 (비평 대상)
- `docs/admin/ARCHITECTURE.md` v0.7 (§ 3 Vertical Slice · § 7 인증·권한 · § 10 미결정)
- `docs/decisions/M0_SCHEMA_PLAN.md` v0.1 (6 core tables · cycle2 schema)
- `docs/decisions/INFRA_DECISIONS_DRAFT.md` v1.0 (Single DB + RLS · Provider · Storage)
- `docs/decisions/PHASE0_WEEK1_SPIKES_DRAFT.md` v1.0 (Spike A/B/C/D/E LOCAL_PASS 패턴)
- `docs/decisions/PACKAGES_STRUCTURE.md` (monorepo 구조 정합)
- `packages/auth/src/*.ts` 전체 (export 시그니처 · cookie · session · resolveTenantContext · withResolvedTenantTransaction · AuthConfig 필드)
- `packages/db/src/*.ts` (withTenantTransaction · service-role · advisory-lock 시그니처)
- `packages/core-content/src/schema.ts` (6 tables · RLS · CHECK · regex)
- `packages/shared-types/src/index.ts` (TenantRole · EffectiveRole · UUID_V4_REGEX · brand types)
- `packages/shared-errors/src/*` (있다면)
- `apps/spike-e/src/**` (Spike E LOCAL_PASS 코드 — Drizzle Adapter shape · 실 magic-link · session 흐름)
- 루트 `package.json` · `pnpm-workspace.yaml` · `tsconfig.base.json`

## 비평 산출물 형식

### A. Finding 리스트

각 finding은 다음 schema로:

```
- id: ADMIN-UI-NN
  severity: major | minor | nit
  location: §N.N 또는 "L<line>" 인용 (plan 본문 또는 SoT 파일 경로)
  issue: "<무엇이 문제인가 — 1~3문장>"
  rationale: "<왜 문제인가 — SoT 직접 인용 또는 코드 인용>"
  suggestion: "<close 권고 · 또는 defer 권고 + 어디로 · 또는 추가 검토 권고>"
  closeableAfterPatch: true | false
```

severity 기준:
- **major**: 명세 결함 · SoT 불일치 · 통합 hole · 보안/RLS/audit 무효화 가능
- **minor**: 일관성 부족 · marker 누락 · 미결정 항목 분류 모호
- **nit**: 문서 표현 · typo · 표 정렬

### B. 누계 시그널

finding 리스트 마지막에 반드시 다음 블록을 출력:

```
=== CYCLE SIGNAL ===
cycle: $N (호출자가 지정)
total_findings: <NN>
major: <N>
minor: <N>
nit: <N>
closeable_after_patch_ratio: <NN>% (closeableAfterPatch=true 비율)
ready_for_acceptance: true | false
scope_narrow_acceptable: true | false
recommendation: "<다음 cycle로 / acceptance로 / scope 축소로>"
```

### C. SoT cascade 영향 (있을 때만)

본 plan 수렴 시 cascade 해야 할 다른 문서를 명시:

```
=== CASCADE ===
- docs/admin/ARCHITECTURE.md § 10 A-01 close → v0.8 patch
- docs/decisions/PACKAGES_STRUCTURE.md apps/web 추가 entry
- 루트 package.json web script 추가
- ...
```

## 중점 검증 항목 (체크리스트)

다음을 빠짐없이 검토:

1. **package 실 코드와 plan 명세 정합**
   - `packages/auth/src/config.ts` `AuthConfig` 필드 ↔ plan § 7 env vars 매핑
   - `packages/auth/src/index.ts` export 시그니처 ↔ plan § 3.2 인증 흐름의 함수 인용
   - `packages/auth/src/resolve-tenant-context.ts` deny reason set ↔ plan § 5.4 에러 → UI mapping의 reason set
   - `packages/core-content/src/schema.ts` clinic_profile CHECK 이름 · 필드 ↔ plan § 6.1 폼 필드 검증 표
   - `packages/db/src/tenant.ts` withTenantTransaction 시그니처 ↔ plan과 (필요 시) packages/auth withResolvedTenantTransaction 의 SET LOCAL 패턴

2. **인증·세션·쿠키 명세 hole**
   - cookie refresh 시점에서 Server Action 응답 vs middleware vs Route Handler의 Set-Cookie 어디서 일어나는지 명확한가
   - super-admin instance switch가 walking skeleton에 안내만 vs 실 동작 — 폴리시 일관성
   - resolveTenantContext의 `requestedInstanceId` 가 URL slug에서 도출될 때 lookup 실패 (404) 와 권한 실패 (403) 분리 정합

3. **RLS 격리 시나리오 (§ 8.1) 의 실효성**
   - cross-tenant URL 변조 시나리오가 실제로 `instance_id` 컬럼 변조까지 시뮬레이션하는가
   - upsert ON CONFLICT 시 `instance_id` 가 SET LOCAL 값과 다르면 어떻게 되는가 (RLS WITH CHECK · USING 정책 정합)
   - session 만료 시 진행 중인 트랜잭션과의 race 가능성

4. **skeleton 범위 vs deferred 분류 합리성**
   - LocationProfile(main) 자동 생성을 defer한 이유가 ARCHITECTURE § 3.8.1 의 "어드민 화면 6개 유지" 원칙과 정합인가
   - LegalDocument 자동 생성을 defer하면서 admin/ARCHITECTURE의 "출시 게이트" 위상이 흔들리지 않는가
   - ComplianceRecord 폼 defer가 admin/ARCHITECTURE § 3.4 "발행 시 ComplianceRecord 자동 생성" 게이트와 충돌하는가 (skeleton에 발행 자체가 없으므로 충돌 없을 가능성 — 검증)

5. **W-01~W-07 미결정 항목**
   - 각 미결정이 cycle1 단발 결정 가능한지 / 별도 spec 필요한지
   - 특히 W-02 next-auth v5 wrapping vs 자체 핸들러 — Spike E 패턴이 자체 핸들러로 PASS인데 next-auth를 굳이 끌어들이는 이유의 정당성

6. **ARCHITECTURE § 10 미결정 close 정합성**
   - A-01·A-02·A-03 close가 admin/ARCHITECTURE v0.7 · INFRA_DECISIONS v1.0과 모순 없는가
   - A-06·A-08 defer가 skeleton 범위와 정합한가

7. **dev experience · 부트스트랩**
   - § 7.1 seed script 결정이 실 사용 가능한가 (CLI args · service-role 필요성 · multi-tenant root insertion 권한)
   - dev mode mock mailbox 노출 (W-05) 의 보안 노출 면

8. **PACKAGES_STRUCTURE · 루트 script · tsconfig cascade**
   - apps/web이 packages 가 아니라 apps인데 `pkg:typecheck` 에 포함 vs 별도 script 의 일관성
   - workspace 의존성 표기 (workspace:*) vs apps/spike-* 의 현재 표기 일관성

9. **운영 시나리오 미커버**
   - 동시 두 운영자 같은 ClinicProfile 저장 race (last-writer-wins 명시 부재?)
   - 매우 큰 description (~5MB) 입력 시 DoS 방어 (Server Action body limit)
   - 잘못된 cookie tampering 시 정확한 audit 기록 경로

10. **typo · 표 정렬 · 일관성**
    - "Phase 0 Week 4" vs "Week 4 M0 vertical slice" 표기 통일
    - § 3.1 디렉토리 트리의 파일 명명 일관성

## 출력 분량 가이드

- finding 15~40 개 사이가 normal
- 한 finding 당 4~7줄
- 마지막 시그널 블록은 짧고 정확하게
- markdown으로 출력

## 시작

CYCLE 번호는 호출자가 지정한 값 사용. 본 호출에서는 cycle 번호가 호출자 prompt에서 명시된다.

지금 비평을 시작한다. 산출물 외 어떤 prose도 출력하지 말 것 (인사 · 결언 등 X).
