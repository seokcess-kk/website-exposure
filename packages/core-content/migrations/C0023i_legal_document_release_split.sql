-- @glitzy/core-content C0023i — LegalDocument release schema split
-- LL-WORKFLOW-INTEGRATION 정합 — body NOT NULL 유지 (자동 생성 안 항상 채워짐). 실 변경 없음.

SELECT 'C0023i: LegalDocument no schema change (body remains NOT NULL · auto-generated always populated)' AS marker;
