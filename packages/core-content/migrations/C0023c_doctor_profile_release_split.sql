-- @glitzy/core-content C0023c — DoctorProfile release schema split
-- photo_url · bio 안 이미 nullable. 실 변경 없음.

SELECT 'C0023c: DoctorProfile no schema change (photo_url/bio already nullable)' AS marker;
