-- @glitzy/core-content C0023g — Publication release schema split
-- journal · doi · pmid · thumbnail_url 안 이미 nullable. 실 변경 없음 — 표기 만.

SELECT 'C0023g: Publication no schema change (optional fields already nullable)' AS marker;
